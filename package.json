{
  "name": "agent-replay",
  "private": true,
  "version": "0.0.0",
  "description": "Time-travel replay for AI agents. Record, scrub, share.",
  "license": "MIT",
  "author": "10xdev4u-alt",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
